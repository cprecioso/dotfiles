#!/bin/sh

run_path() {
  echo "$1/waspc/run"
}

repo_dir="$(git rev-parse --show-toplevel)"
if [ ! -x "$(run_path "$repo_dir")" ]; then
  repo_dir="{{.chezmoi.config.destDir}}/Developer/Wasp/wasp"
fi

echo "Using wasp at ${repo_dir}..." >&2
exec "$(run_path "$repo_dir")" "$@"
