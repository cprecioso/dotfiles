# shellcheck shell=bash

# iTerm integration
zstyle ':omz:plugins:iterm2' shell-integration yes

# Disable ohmyzsh auto-update, since we have an auto-update script for antidote
zstyle ':omz:update' mode disabled
