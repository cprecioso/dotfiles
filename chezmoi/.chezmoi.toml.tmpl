{{- $personal := promptBoolOnce . "personal" "Add Personal files" -}}
{{- $remote   := promptBoolOnce . "remote"   "Add Remote files"   -}}

[data]
  personal = {{ $personal | toToml }}
  remote   = {{ $remote   | toToml }}
